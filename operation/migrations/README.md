Empty on purpose: the five migrations that used to live here (content list-id, flow counts, content has-face, content trigger dedup, user post-count) were folded into `admin/src/services/tenant-init-sql.ts`'s `TENANT_DB_INIT_SQL` when user/content moved back to per-tenant D1 (2026-07-26 restore). Future per-tenant schema changes go here as new `TenantMigration` files (see `../migrate-tenant-dbs.ts`).
